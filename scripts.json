{
  "scripts": {
    "android": "react-native run-android",
    "ios": "react-native run-ios",
    "lint": "eslint .",
    "start": "react-native start",
    "test": "jest",
    "build:android:dev": "cd android && ./gradlew assembleDebug",
    "build:android:prod": "cd android && ./gradlew assembleRelease",
    "bundle:android:dev": "react-native bundle --platform android --dev true --entry-file index.js --bundle-output android/app/src/main/assets/index.android.bundle --assets-dest android/app/src/main/res/",
    "bundle:android:prod": "react-native bundle --platform android --dev false --entry-file index.js --bundle-output android/app/src/main/assets/index.android.bundle --assets-dest android/app/src/main/res/",
    "ota:export:dev": "npm run bundle:android:dev && cp android/app/src/main/assets/index.android.bundle android/output/",
    "ota:export:prod": "npm run bundle:android:prod && cp android/app/src/main/assets/index.android.bundle android/output/",
    "ota:deploy:dev": "npm run ota:export:dev && git add android/output/index.android.bundle && git commit -m 'Deploy OTA update to development' && git push origin development",
    "ota:deploy:prod": "npm run ota:export:prod && git add android/output/index.android.bundle && git commit -m 'Deploy OTA update to production' && git push origin production"
  }
}
